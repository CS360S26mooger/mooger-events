<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginBottom="8dp"
    app:cardCornerRadius="14dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <!-- Clock icon -->
        <ImageView
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:src="@drawable/ic_nav_history"
            android:tint="#C96B8E"
            android:layout_marginEnd="10dp"
            android:contentDescription="Time" />

        <!-- Time + status on one row -->
        <TextView
            android:id="@+id/textSlotTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="00:00"
            android:textSize="15sp"
            android:textStyle="bold"
            android:textColor="#212121"
            android:layout_marginEnd="10dp" />

        <TextView
            android:id="@+id/textSlotStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Available"
            android:textSize="10sp"
            android:textStyle="bold"
            android:textColor="#3A7D5A"
            android:background="@drawable/bg_tag_pastel"
            android:backgroundTint="#E8F5EF"
            android:paddingHorizontal="6dp"
            android:paddingVertical="2dp" />

        <!-- Push delete button to the far right -->
        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Delete button -->
        <ImageButton
            android:id="@+id/buttonDeleteSlot"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:src="@drawable/ic_trash"
            android:tint="#C62828"
            android:background="@drawable/circle_bg"
            android:backgroundTint="#FFEBEE"
            android:padding="8dp"
            android:contentDescription="Delete slot" />

    </LinearLayout>

</androidx.cardview.widget.CardView>
